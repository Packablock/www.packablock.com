# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Packablock
email: support@packablock.com
description: >-
  The Deterministic Supply Chain Policy Control.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://www.packablock.com" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: packablock
github_username:  Packablock

# Social & SEO Sharing
image: /assets/images/packablock-social-card.png
logo: /favicon.svg

twitter:
  username: packablock
  card: summary_large_image

social:
  name: Packablock
  links:
    - https://github.com/Packablock
    - https://www.youtube.com/@packablock
    - https://twitter.com/packablock

# Build settings
theme: minima

# Primary navigation pages
header_pages:
  - about.markdown
  - cra-compliance-checklist.markdown
  - reports.markdown

# Collections
collections:
  reports:
    output: true
    permalink: /reports/:slug/

# Site-wide Front Matter Defaults
defaults:
  - scope:
      path: ""
    values:
      image:
        path: /assets/images/packablock-social-card.png
        height: 630
        width: 1200
        alt: "Packablock — Package Control, Block by Block."

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/

plugins:
  - jekyll-sitemap
